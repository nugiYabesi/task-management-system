# Copy this file to .env and fill in your PostgreSQL credentials
# (the ones you use to log in through pgAdmin)

DB_HOST=localhost
DB_PORT=5432
DB_NAME=task-manager
DB_USER=postgres
DB_PASSWORD=your_password_here

PORT=5000
